# =============================================================================
# FILE: setup/requirements.txt
# PURPOSE: Python dependencies for Snowflake Master Course
# INSTALL:  pip install -r setup/requirements.txt
# PYTHON:   3.9, 3.10, or 3.11 recommended
# =============================================================================

# -----------------------------------------------------------------------------
# Core Snowflake connectivity
# -----------------------------------------------------------------------------

# Snowflake Python connector — low-level connection, cursor, COPY commands
snowflake-connector-python[pandas]==3.6.0

# Snowpark for Python — DataFrame API running inside Snowflake
snowflake-snowpark-python[pandas]==1.20.0

# Snowflake ML — feature store, model registry, Cortex ML functions
snowflake-ml-python==1.5.0

# -----------------------------------------------------------------------------
# Application framework
# -----------------------------------------------------------------------------

# Streamlit — used in chapter_12 for Streamlit-in-Snowflake apps
streamlit==1.32.0

# -----------------------------------------------------------------------------
# Data manipulation
# -----------------------------------------------------------------------------

# pandas — DataFrame operations, read_sql, result set handling
pandas==2.2.0

# numpy — numerical computing, used alongside pandas
numpy==1.26.4

# scikit-learn — ML model training examples in chapter_13
scikit-learn==1.4.0

# -----------------------------------------------------------------------------
# Visualisation
# -----------------------------------------------------------------------------

# Altair — declarative charting library used in Streamlit apps
altair==5.2.0

# -----------------------------------------------------------------------------
# Database / ORM
# -----------------------------------------------------------------------------

# SQLAlchemy — used with snowflake-sqlalchemy for ORM-style queries
sqlalchemy==2.0.28

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------

# cryptography — required for key-pair authentication with Snowflake
cryptography==42.0.4

# -----------------------------------------------------------------------------
# Configuration
# -----------------------------------------------------------------------------

# python-dotenv — load .env files for local credential management
python-dotenv==1.0.1

# -----------------------------------------------------------------------------
# DevOps / CI tools
# -----------------------------------------------------------------------------

# dbt-snowflake — dbt adapter for Snowflake (installs dbt-core as dependency)
dbt-snowflake==1.8.0

# schemachange — lightweight Snowflake migration tool (chapter_16)
schemachange==3.6.0
